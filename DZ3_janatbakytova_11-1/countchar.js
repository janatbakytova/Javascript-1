// Написать функцию countChar, которая может считать длину переданной как аргумент строки
// и вывести в консоль длину.

let letter = prompt("Write something here!");
alert("The word you typed takes " + letter.length + " code values!")

let empty = " ";
alert("The spaces you entered exactly " + empty.length);

